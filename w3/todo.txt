Week 3
- HTML 12–18（表单控件/提交）；JS 12–16、20–24（函数/作用域、JSON、DOM/表单）

- 注册/登录表单 Demo（纯原生 HTML/CSS/JS）
- 即时校验：用户名/邮箱/密码/确认/条款，错误提示就地可视化
- 通过后把表单数据序列化为 **JSON** 并在页内只读展示（本周先不真实请求）

- 提交材料：代码仓库链接 + 页面挂载链接

- 加分项：`POST /api/login` + Session；**拦截器**拦截未登录的写操作（返回 401）
- `GET /api/users` 支持 `keyword` 模糊搜索或 `sort=createdAt,desc`
- 前端头像文件选择并本地预览（FileReader）